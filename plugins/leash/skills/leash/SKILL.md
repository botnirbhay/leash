---
name: leash
description: Declare or disable a deterministic edit scope for this Codex session.
---
# Leash
Use `leash scope src/auth/**` to block edits outside that scope. Use `leash off` to disable Leash for the current project and session. A leading `$` is also accepted.
